#ifndef utils_H_
#define utils_H_  

    #include "define.h"
    #define DEFAULT_NAME "No name list"

    int str_rename(char **, const char *);
    int reset(void *);
    int perror_return(const char *, int);

#endif